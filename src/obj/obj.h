#ifndef IB_OBJ
#define IB_OBJ

/*
 * iceberg has no concept of objects
 * instead we provide functions to ib_world which are bound to object names
 * objects are constructed with a typename and properties
 */

void ib_obj_bind_all();

#endif
